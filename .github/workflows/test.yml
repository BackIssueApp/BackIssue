# Run the test suite on every pull request and push to main — the check that
# branch protection requires before a PR can merge.
name: Tests

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      # Build the UI FIRST — the server serves frontend/dist (gitignored), and
      # shell-serving tests assert against it.
      # npm install (not ci): the lockfile is generated on Windows and omits
      # rolldown's per-platform native binding for linux — same as the Dockerfile.
      - run: npm install --prefix frontend && npm run build --prefix frontend
      - run: npm test
