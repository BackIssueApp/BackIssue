node_modules/
# Built UI (npm run build → frontend/dist)
frontend/dist/
.profile/
downloads/
*.db
*.db-journal
*.db-shm
*.db-wal
# Plugin-maintained cover cache (lives beside the db)
bw-covers/
fixtures/
.superpowers/
.staging/
debug/
settings.json
# External plugins (private download sources) live here, each its own repo.
plugins/
# Download sites live in their own repository, cloned into sources/.
sources/
# Rendered page images the reader keeps on disk — generated data, not source.
cache/

# Docs (internal)
docs/

# Node fatal-error diagnostic reports
reports/

# ML models (large binaries, distributed separately)
models/

# dev-server output logs
*.log
